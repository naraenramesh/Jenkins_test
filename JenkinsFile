pipeline {
 agent any

 stages{

  stage('Install Dependencies'){
    steps{
    sh "yarn"
    echo "Yarn installed"
    }
  }
  stage('Run Tests'){
    steps{
    sh "yarn test"
    echo "Test results"
    }
  }
 }
}
