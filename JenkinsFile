pipeline {
 agent any

 stages{

  stage('Install Dependencies'){
    steps{
    sh "npm install --global yarn --unsafe-perm=true --allow-root"
    sh "yarn install"
            echo "Yarn installed"
  }
  }
  stage('Run Tests'){
    steps{
    sh "yarn test"
    echo "Test results"
    }
  }
 }
}
