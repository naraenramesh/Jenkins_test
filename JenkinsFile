pipeline {
 agent any

 stages{

  stage('Install Dependencies'){
    steps{
    dir("./psnl_projects/jenkins_test") {
  sh "which yarn"
            echo "Yarn installed"

    }
  }
  }
  stage('Run Tests'){
    steps{
    sh "yarn test"
    echo "Test results"
    }
  }
 }
}
