pipeline {
 agent any

 stages{

  stage('Install Dependencies'){
    steps{
    sh "uname -an"
            echo "Yarn installed"
  }
  }
  stage('Run Tests'){
    steps{
    sh "yarn test"
    echo "Test results"
    }
  }
 }
}
