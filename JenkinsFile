pipeline {
 agent any

 stages{

  stage('Install Dependencies'){
    steps{
    sh "npm install -g yarn"
    sh "yarn install"
            echo "Yarn installed"
  }
  }
  stage('Run Tests'){
    steps{
    sh "yarn test"
    echo "Test results"
    }
  }
 }
}
